---
const needs = [
  {
    type: "Básica",
    icon: "💚",
    title: "Cuida tu bienestar",
    description: "Opciones de antojo más cuidadas y porciones controladas para disfrutar sin culpa."
  },
  {
    type: "Latente",
    icon: "🎌",
    title: "Experiencia diferente",
    description: "Vive una experiencia culturalmente atractiva que rompe con lo ordinario."
  },
  {
    type: "Explícita",
    icon: "⚡",
    title: "Acceso rápido",
    description: "Postres y bebidas japonesas sin ir a un restaurante. Siempre disponible."
  },
  {
    type: "Implícita",
    icon: "✨",
    title: "Calidad premium",
    description: "Frescura constante con presentación premium en cada producto."
  },
  {
    type: "Derivada",
    icon: "⏱️",
    title: "Ahorro de tiempo",
    description: "Compra en segundos, sin filas ni horarios. Tu antojo cuando lo necesites."
  }
];
---

<section class="py-16 md:py-24 bg-gradient-to-br from-sakura/10 via-white to-matcha/10">
  <div class="container px-4">
    <div class="text-center mb-12 md:mb-16">
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-display font-bold text-carbon mb-4">
        Hecho para tus <span class="text-vermillion">antojos</span>… y tu <span class="text-matcha">tiempo</span>.
      </h2>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto">
      {needs.map((need, index) => (
        <div class={`bg-white rounded-mochibox-lg p-8 shadow-md hover:shadow-xl transition-all duration-300 border-t-4 ${
          index === 0 ? 'border-matcha' : 
          index === 1 ? 'border-sakura' : 
          index === 2 ? 'border-vermillion' : 
          index === 3 ? 'border-carbon' : 
          'border-matcha'
        } ${index === 2 ? 'lg:col-span-3 lg:max-w-md lg:mx-auto' : ''}`}>
          <div class="flex items-start gap-4">
            <div class="text-4xl flex-shrink-0">
              {need.icon}
            </div>
            <div>
              <div class="text-xs font-semibold text-carbon/50 uppercase tracking-wider mb-1">
                {need.type}
              </div>
              <h3 class="text-xl font-display font-semibold text-carbon mb-2">
                {need.title}
              </h3>
              <p class="text-carbon/70 leading-relaxed">
                {need.description}
              </p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
